timeout 180 "brightnessctl -qsc backlight set 10%" resume "brightnessctl -r >/dev/null"
timeout 300 "locker || waylock -fork-on-lock"
timeout 330 "wlopm --off '*'" resume "wlopm --on '*'"
timeout 1200 "playerctl -a status | grep Playing || doas -n /usr/bin/zzz"
before-sleep "locker || waylock -fork-on-lock"
