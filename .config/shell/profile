#!/bin/sh

set -eu

. "$HOME/.config/shell/env"

[ -r "$XDG_CONFIG_HOME/loadkeys/loadkeysrc" ] &&
	doas -n /usr/bin/loadkeys "$XDG_CONFIG_HOME/loadkeys/loadkeysrc"

[ -z "$WAYLAND_DISPLAY" ] &&
	[ "$(tty)" = "/dev/tty1" ] &&
	exec "$XDG_CONFIG_HOME/river/run"
