#!/bin/sh

dir="${1:-$HOME}"
depth="${2:-1}"

sel="$(find "$dir" -mindepth "$depth" -maxdepth "$depth" -type d -printf "%T@ %P\n" |
	sort -nr | cut -d " " -f 2- | dmenu -r -i -fn monospace-12 -l 10 -p "dir:")"

cd "$dir/$sel" && setsid -f "$TERMINAL"
