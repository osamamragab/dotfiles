#!/bin/sh

set -eu

pidof -sqx gpg-agent && pkill -x -HUP gpg-agent || true

exec waylock \
	-fork-on-lock \
	-log-level info \
	-ignore-empty-password \
	-init-color 0x2e3440 \
	-input-color 0x3b4252 \
	-fail-color 0xbf616a
