#!/bin/sh

set -eu

OPEN_HISTORY_FILE="${OPEN_HISTORY_FILE:-${XDG_DATA_HOME:-$HOME/.local/share}/open_history.txt}"
[ -r "$OPEN_HISTORY_FILE" ] || exit 1

sel="$(tac "$OPEN_HISTORY_FILE" | menu -w 70 -p "recents> ")" || exit 1
exec open "$sel"
